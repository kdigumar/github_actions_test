---
name: Main Workflow
on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - main
jobs:
  select-environment:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: systest
